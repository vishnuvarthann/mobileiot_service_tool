import 'package:flutter/material.dart' ;
import 'package:flutter/widgets.dart';




class SignOut extends StatelessWidget {
   @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar: AppBar(
            title: Text("SignOut"),
            ),
        body: Center(child: Text('SignOut')));
  }
  
}